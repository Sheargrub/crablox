
/*
pub enum Expression {
    L(Literal),
    U(Unary),
    B(Binary),
}

pub enum Literal {
    Number(f64),
    String(f64),
    True,
    False,
    Nil,
}

pub enum Unary {
    Negative(Expression),
    Not(Expression),
}

pub struct Binary {
    Left: Expression,
    Operator: BinaryOp,
    Right: Expression,
}

pub enum BinaryOp {
    Equal,
    NotEqual,
    Less,
    LessEqual,
    Greater,
    GreaterEqual,
    Add,
    Subtract,
    Multiply,
    Divide,
    Modulo,
}*/